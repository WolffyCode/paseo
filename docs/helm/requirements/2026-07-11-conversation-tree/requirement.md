# 需求 · 左侧对话树(项目 → 对话 → subagent 无限嵌套 · 主机切换器 · 桌面 only)

> 日期:2026-07-11(状态点 / 图标二轮修正:2026-07-23;对话行 / 项目行信息架构三轮修正:2026-07-23) · 状态:闸 1 已通过(2026-07-11,六项开放问题按推荐值采纳);状态点 / 图标二轮修正已获董事长拍板(2026-07-23,方案 D);对话行 / 项目行信息架构三轮修正已获董事长拍板(2026-07-23,详见 §8;其中两项开放问题已于同日确认收口,另有若干项待架构 / 产品确认)· 闸 3 验收中 · 配套 UI:[ui.html](./ui.html) · canonical:docs/helm/product.md
> **只写 WHAT**(见 [standards.md](../../standards.md) §7)——全文不含组件名 / 函数名 / 文件名 / store 名 / 路由名 / 协议名 / RPC 名 / 代码片段 / 实现路子。
> **锚点约定**:与 UI 设计师并行,本文 §4 定义的屏用 `sCT1`…`sCTn`(Conversation Tree)前缀,requirement 每状态 ↔ ui.html 每屏 1:1。
> **定位**:本需求填的是壳子 `2026-06-28-shell` 里「左侧边栏」这个空容器的**内部内容**——兑现该需求 §7.b 第 7–18 条(host 切换器 / 新对话 / 搜索 / 对话树全部细节 / 右键 / 重命名 / 归档剥离 / 关页签语义),并落实 `2026-06-27-codepilot-layout` §5「左栏外壳 vs 内部」硬边界里「内部 = Helm 现有对话树那套」的判决。**不重定义**壳行为(左栏开关 / 默认 240px / 拖 180–300 / 记忆宽度 / 设置入口位置)——那些一概沿用壳子与 codepilot-layout,本需求只管左栏内部装什么内容。
> **忠实还原基线声明**:本需求的树展示 / 行交互 / 右键菜单等,以 **Paseo 当前真实实现**(左侧边栏对话树相关代码)为准描述——这是已上线、跑过用户反馈打磨的真实行为,不是概念稿。`2026-06-25-home-shell` 的对话树设计稿(#s6 / #s7 等)仍处 gate-1、部分内容(按节点类型三分式右键菜单、拖拽排序等)**尚未落地**,本需求把它们当作「参考灵感与后续方向」,不当作现状;两者有出入之处,正文逐条标注「现状」与「home-shell 构想 · 本轮不做」,不混为一谈。**两处例外(重新设计,非现状复用)**:① 图标 + 状态点 + 对话行 / 项目行结构——现状树行**未渲染**任何状态点(此前有过近似尝试,因反馈「达不到想要的效果」被整体移除),本轮基于既有真实数据重新设计;闸 1 通过后,董事长两次追加修正意见(2026-07-23 视觉修正「方案 D」→ 同日信息架构修正),**当前生效的是最新一轮**:对话(根)行不再有机器人图标、也不再有状态点,前置图形只剩展开箭头,内容为两行(标题 + 最近说话相对时间 / 标签行:CLI 类型标签 + 当前状态标签);subagent 行去机器人图标、保留小号状态点、维持单行不变;项目行升级为两行(文件夹图标 + 标题 / 当前分支 + diff 统计)。历次决策、理由与仍未决的数据依赖见文首「本次修订」记录与 §2·A、§2·B、§8;② hover / 选中的两档灰阶——现状 hover 与选中共用同一档灰,本轮改为两档,是相对现状的有意修正,见 §2·C、§8。两处均已获董事长确认,决策记录见 §8。
> **本次修订(闸 1 对齐 · 2026-07-11)**:按总监对齐审意见收口——① 屏号按 UI 定稿统一为 **7 屏**(`sCT1`–`sCT7`):原 sCT4「右键菜单」并入 sCT3(项数项名原样保留),原「点击联动」与「主机切换器」两屏互换顺序(主机切换器提前),原 sCT8 顺延为 sCT7,§4 / §6 / §7 / §8 的锚点引用同步更新;② hover / 选中改两档灰(见 §2·C),列入开放问题;③ 状态点定性为**重新设计**而非**现状复用**(见 §2·B),新增开放问题请董事长闸 1 特别过目视觉效果;④ sCT6(点击联动)里 subagent → 右侧的表述补强为「发起侧就绪 · 承接侧(右栏「对话」页签类型)deferred」的诚实边界,沿用 `2026-07-07-right-sidebar` 的断链表述范式(见 §2·G / §5)。全文其余内容未改。
> **本次修订(状态点 / 图标二轮修正 · 2026-07-23)**:闸 1 通过后,董事长在总监的直接对话里针对 §2·B「状态点视觉」这一项追加一轮修正意见——现状机器人图标与状态点堆在一起显得潦草、状态点的光环效果也没有达到预期,与总监反复打磨可视化方向稿(A/B/C/D 四个方向)后明确拍板采用**方案 D**。改动范围严格限定在「图标 + 状态点 + 对话(根)行是否两行」,不触及展开箭头 / 缩进逻辑 / 项目行结构 / 两套右键菜单 / hover 尾部动作 / 内联重命名 / closed 语义 / 拖拽:① **去机器人图标**——对话 / subagent 节点不再显示机器人图标,项目节点的文件夹图标不受影响(见 §2·A);② **状态点改为无光环的纯色实心圆**,运行中 / 待输入或需关注 / 初始化中三态由状态点自身透明度呼吸表达动效,空闲 / 出错两态静止(见 §2·B);③ **对话(根)节点行由单行改两行**——第一行标题 + 角标,第二行灰色小字状态文案直接读出状态,「待输入或需关注」态按触发原因拆成「等待权限确认」/「等待你的回复」两种文案(见 §2·B);④ **subagent 节点保持单行**,同样去图标、仅留(更小号的)状态点 + 标题,不跟随对话(根)升级两行(见 §2·B);⑤ 明确记录代价——对话(根)行高度从现状单行(约 30px)变为两行(预计 46–48px 量级,精确值以 UI 定稿为准),同一纵向空间可见对话行数因此下降约四成,是有意识的取舍而非被忽略的副作用(见 §2·B、§6)。**唯一未决项**:空闲态「相对最后活跃时间」后缀依赖一个当前对话树数据模型尚不具备的时间戳字段,能否本轮接入待架构确认,列入 §8 开放问题第 7 条,不假定其已存在。全文其余内容(含 2026-07-11 通过的六项开放问题结论)未改。
> **本次修订(信息架构三轮修正 · 2026-07-23)**:董事长看过 UI 出的实际渲染页面后,认为两行结构方向对,但内容需要再深一层——这轮改的是对话行 / 项目行的**信息架构**,不只是视觉细节:① **对话(根)行前置状态点也去掉**——上一轮已去机器人图标、留了状态点,这轮状态点本身也去掉,前置图形位置只剩展开箭头(见 §2·A、§2·B);② **对话(根)行第二行从纯文字状态描述改为「标签行」**——一排独立标签,至少含 **CLI 类型标签**(该对话由哪个 agent provider 驱动)与**当前状态标签**(复用既有五态语义,只是从「点」的视觉改成「文字」),「至少」意味着不排斥后续再加;③ **对话(根)行第一行行尾从「子对话计数角标」改成「最近说话相对时间」**——原有 subagent 计数角标按董事长原话直接去掉、不换到别处,这处信息去留的可用性顾虑单独列为开放问题(见 §8 开放问题 9),不擅自加回也不擅自咽下;④ **项目行从「本轮不动」改为「也升级两行」**——第一行维持文件夹图标 + 标题不变,新增第二行 = 当前 git 分支 + diff 统计,无分支时显示「暂无分支」(见 §2·A、§2·B)。**两处数据依赖已重新独立核实,不是照抄董事长的初步判断**:CLI 类型标签所需的 provider 归属信息、「最近说话时间」所需的活跃时间戳,现状都是「协议层大概率已有、这一层对话树模块没有解出来用」,不是「完全不存在」,但能否本轮接入仍需架构确认(见 §8 开放问题 8、7)。「最近说话时间」的地位从上一轮「空闲态的可选后缀」升级为「每一行都要展示的核心信息」,§8 开放问题 7 的措辞与优先级同步上调为**阻塞性**。分支 / diff 已确认复用既有数据(工作区详情模型已有对应字段、hover 工作区卡片已在用同一数据、也已处理无分支的情况),但把它们从「仅 hover 时计算 / 展示」改成「行内常显」是否有渲染 / 滚动性能代价,不由本文档判断,已转交架构(见 §8 开放问题 11)。subagent 行本轮**未被提及**,继续维持上一轮定义(单行 · 小号状态点 · 无机器人图标)不变;但对话(根)行去点、subagent 行留点,同一棵树内五态语义的视觉表达从此不统一,这一张力已独立列为开放问题(见 §8 开放问题 10),不擅自决定是否同步升级 subagent。全文其余内容(含此前两轮结论)未改。

## 1. 目标 & 用户价值

**一句话目标**:把主界面左侧边栏的空容器,填成一个可用的**对话树**——按项目分组、可无限嵌套 subagent、可搜索、可新建、可管理、可跨主机切换的会话导航区,让用户在一处看清「本机 + 远程主机上有哪些项目、每个项目下有哪些对话、每条对话内部还派生了哪些 subagent、谁在跑、谁需要我」。

**用户为什么需要**:

- 用户随时随地(含手机盯着本机)监控 / 指挥多个 AI 编码 agent,**左栏是唯一的导航起点**——不点进某条对话,用户无法看到它的内容、无法知道它是否还在跑、是否出错、是否需要输入。没有这块,整个 app 不可用。
- 真实工作中,一条任务经常在内部派生 subagent(Explore 子任务、写测试子任务……),用户需要在**同一棵树**里看清「谁是谁的子任务」,而不是所有对话拍平在一层、丢失父子关系。
- 用户手上不止一台主机(本机 + 公司电脑 + 云服务器等),需要**不切工具、不重连**地在几台主机的对话之间跳转,由树顶部的入口提供。
- 项目一多,列表会变长;**搜索**让用户不用滚动就能定位到想找的那条对话。
- **本轮把这一整块从「壳里的空容器」变成「真实可用」**——`2026-06-28-shell` 需求刻意把所有内容留空以求壳先稳定;本需求正是那份「后续内容里程碑」承诺的兑现者之一。

## 2. 范围

### 做

#### A. 对话树展示(项目 → 对话 → subagent 无限嵌套)

- **分组**:纵向三组——**置顶**(仅当存在置顶项目 / 对话时出现,置顶项目整棵进入、置顶对话从其所属项目里提升出来)→ **项目**(按代码仓库聚合的分组;同一项目下可能同时有主目录与若干工作树,其上的对话都归在同一项目节点下)→ **对话**(未归入任何项目的游离对话)。
- **嵌套**:项目展开出其下对话(根);对话展开出其 subagent;subagent 可以再展开出更深一层 subagent,递归、**不设人为层数上限**。视觉缩进达到一定深度后**停止继续增长**(深层级仅靠展开箭头与父子关系判断——对话 / subagent 节点已不显示机器人图标,不能再靠图标区分层级,见下方「节点组成」2026-07-23 追加修正),但父子关系与展开 / 收起能力不受影响。
- **节点组成(2026-07-23 追加两轮修正——视觉方案 D → 信息架构修正,按节点类型分述,详见 §2·B)**:项目节点 = **两行**——第一行(不受本轮影响)= 展开箭头(仅当有可展开内容时出现,前置于图标之前,收起 / 展开态图形不同,如旋转)+ 文件夹图标 + 标题(单行、超长省略);第二行(信息架构修正新增)= 当前 git 分支 + diff 统计,无分支时显示「暂无分支」(见 §2·B;该分支具体取项目下**哪个目录**的分支——项目本身聚合了主目录与若干工作树,各自可能在不同分支上,本文档尚未显式定义,见 §8 开放问题 12)。对话(根)节点 = 展开箭头(如有子代)+ **两行**——第一行「标题(单行、超长省略)+ 最近说话相对时间(行尾,替代原后代总数角标)」+ 第二行「标签行:至少含 CLI 类型标签 + 当前状态标签」——**前置图形位置只剩展开箭头**:机器人图标上一轮已去,状态点本轮也一并去掉,该节点的运行状态改由第二行「当前状态标签」以文字呈现,不再有独立的点(见 §2·B)。subagent 节点 = 展开箭头(如有子代)+ 状态点(小号纯色实心圆)+ 标题(单行、超长省略)+ 后代总数角标(拥有后代时,逻辑不变)——**不再显示机器人图标**,且**保持单行**,本轮反馈未提及 subagent、原样维持,不跟随对话(根)升级两行(subagent 缩进更深、同一对话下可能同时存在多个,两行占用纵向空间过多;是否应因视觉一致性同步调整,董事长已确认**维持现状、不强行统一**,定性为有意的信息密度梯度,见 §8 开放问题 10)。
- **空态细节**:项目节点展开但其下没有任何对话时,显示「暂无对话」的缩进提示,而非空白或消失;对话 / subagent 节点只有真的存在子节点时才显示展开箭头,无子节点的行以等宽占位对齐(项目行对齐文件夹图标,对话(根)行对齐标题起始位置——该行已无图标或状态点,subagent 行对齐状态点),不显示箭头。

#### B. 状态点、标签行与对话 / 项目行结构(重新设计,非现状复用)

- **本条是重新设计,不是「忠实还原现状」;董事长在闸 1 基础上追加了两轮修正,范围一轮比一轮深**:闸 1 通过时(2026-07-11)本条只重新设计了状态点视觉本身——当前树行**未渲染**任何状态点,此前曾有过近似尝试,因反馈「达不到想要的效果」被整体移除;节点的运行状态数据(五值状态枚举 + 「需关注」标志)本身真实存在,只是没有对应的树内视觉。闸 1 通过后,董事长先追加**视觉修正**(2026-07-23,方案 D:去机器人图标、状态点改无光环纯色实心圆、对话(根)行由单行改两行);看过 UI 实际渲染页面后,又追加**信息架构修正**(同日):对话(根)行的状态点本身也去掉,第二行从纯文字状态描述改为「标签行」,第一行行尾从子对话计数角标改为最近说话相对时间,项目行也升级为两行。**下方各条描述的是最新一轮(信息架构修正)生效后的最终状态,不是逐层历史**——历史演变见文首「本次修订」记录。**这些改动均已拍板,不是待议的开放问题**;仍待架构 / 产品确认的是几项数据依赖(见 §8 开放问题 7、8、11、12),子对话计数去留与视觉一致性两项顾虑董事长均已确认收口(见 §8 开放问题 9、10)。
- **图标**:对话 / subagent 节点**不再显示机器人图标**;项目节点的文件夹图标不受影响、保持现状。
- **对话(根)行不再有状态点**:上一轮视觉修正保留了状态点(只去了机器人图标);本轮信息架构修正把状态点本身也去掉——对话(根)行前置图形位置现在只剩展开箭头(如有子代),没有图标也没有点。该节点的运行状态改由第二行「标签行」里的「当前状态标签」以文字呈现(见下方「标签行构成」)。
- **状态点渲染(现仅适用于 subagent)**:对话(根)行已不再有状态点(见上条);subagent 节点的状态点是干净的纯色实心圆,**不带外圈光环 / 阴影光晕**——现状实现里那圈模糊光晕效果不够干净,早期 ui.html 稿子里设想的同色光环也被认为是多余的装饰,两者均被否决,做小号纯色实心点即可。
- **动效(现仅适用于 subagent)**:subagent 节点处于**运行中 / 待输入或需关注 / 初始化中**三态时,状态点以**自身的透明度呼吸**表达动效(替代此前设想的外圈光环动画);**空闲 / 出错**两态静止,不做动画。对话(根)行没有状态点可言动效;其「当前状态标签」是否需要额外的颜色 / 强调,遵循应用既有标签 / 徽标的视觉规范,本需求不为其规定新样式。
- 对话 / subagent 节点的运行状态至少区分五态:**运行中** / **待输入或需关注**(涵盖「回合结束等待用户」与「完成后需要人复核」两种触发原因,统一为同一个可见信号)/ **空闲** / **出错** / **初始化中**。五态语义本身不因视觉表达方式不同而改变——subagent 用状态点(见上两条),对话(根)用第二行标签行里的文字标签(见下方「当前状态标签」)。
- 该套语义与应用内其它活动指示(如终端活动)**同源**——复用既有的「运行 / 待输入 / 空闲 / 出错」活动词汇体系,不为树自造一套新状态名。
- **对话(根)行两行结构**:对话(根)节点的行由单行改为**两行**——第一行 = 标题(单行、超长省略)+ 最近一次消息往来的相对时间(行尾,见下方「最近说话时间」);第二行 = **标签行**(见下方「标签行构成」),不是一整句状态描述文字。两行同属一个交互单元——hover、选中、双击重命名等既有行为覆盖整行,不因拆成两行而改变判定方式或拆分成两个可交互区域。
- **标签行构成**:第二行由若干独立的标签(标签之间不连成一句话)组成,至少包含两个——**CLI 类型标签**与**当前状态标签**(见下两条)。「至少」意味着不排斥后续再加其它标签,本需求不认为这两个已经封顶;标签本身的视觉呈现(颜色、圆角、内边距等)遵循应用既有的标签 / 徽标视觉规范,本需求不为其规定新样式。
- **CLI 类型标签**:显示驱动该对话的 agent provider(本项目当前支持 Claude Code / Codex / GitHub Copilot / OpenCode / Pi)。这项数据当前能否从对话树自身的数据模型取得尚待架构确认(见 §8 开放问题 8),不假定其已经可用。
- **当前状态标签**:以文字呈现上方五态语义,文案至少覆盖——**运行中** → 「运行中」;**待输入或需关注**态由待处理的权限确认触发时 → 「等待权限确认」,由其它触发原因(回合结束等待用户、完成后需要人复核)触发时 → 「等待你的回复」;**空闲** → 「空闲」;**出错** → 「出错」;**初始化中** → 「初始化中…」。这些文案所需的数据(运行状态、是否需关注、触发原因、待处理权限数量)复用既有字段,是现成的、不是新缺口。
- **最近说话时间(第一行行尾,替代原后代总数角标)**:显示该对话最近一次消息往来的相对时间(如「3 分钟前」),不分状态、每一行都要展示,不再是仅空闲态才有的可选后缀。这项数据当前对话树自身的数据模型是否已具备,尚待架构确认,且是**阻塞性依赖**(见 §8 开放问题 7)——地位不同于上一轮「空闲态可选后缀」,本轮要求架构给出明确结论,不能延后为不确定的后续待办。
- **子对话(subagent)计数角标去留**:对话(根)行不再显示子对话总数角标——按董事长原话直接去除,**不移到别处**(第一行行尾的位置已经换成「最近说话时间」)。这一信息去留的可用性顾虑董事长已确认收口(见 §8 开放问题 9)——维持去掉、不找补,理由是 subagent 的需关注信号已经体现在其所属对话自身的状态里,丢的只是「具体几个」这个价值不大的数字。
- **subagent 保持单行**:subagent 节点**不**采用两行结构,继续单行呈现——subagent 缩进更深、同一对话下可能同时存在多个,两行会占用过多纵向空间。subagent 行同样不显示机器人图标,只保留(小号的)状态点与标题(单行、超长省略);后代总数角标逻辑不变,拥有后代时才显示。本轮反馈未提及 subagent,原样维持上一轮定义;对话(根)行已去状态点、subagent 行仍保留状态点,同一棵树内五态语义的视觉表达因此不统一——董事长已确认这是**有意的信息密度梯度**(项目最重 · 对话次之 · subagent 最轻),不是疏漏,不需要同步调整 subagent(见 §8 开放问题 10)。
- **项目行两行结构**:项目节点的行由单行改为**两行**——第一行(不受影响)= 展开箭头 + 文件夹图标 + 标题(单行、超长省略);第二行(新增)= 当前 git 分支,若该工作区存在未提交的改动,分支名后附加增删行数统计(复用 hover 工作区卡片已在用的「+增 -删」呈现,不新造一套);无改动时只显示分支名,不叠加「+0 -0」这类噪音。**无分支时显示「暂无分支」**,不叠加显示 diff 统计。这项数据本身不是新缺口——工作区详情模型已有对应字段(可空的分支、可空的增删统计),现有的 hover 工作区卡片已在用同一数据、也已处理「无分支」的情况;这轮变化的性质是把「原本只在 hover 时才展示」的信息改成「项目行内常驻显示」。但常驻显示是否会给长列表的渲染或滚动带来额外性能代价,不归本文档判断,已转交架构评估(见 §8 开放问题 11)。**这一处「当前分支」具体取项目下哪个目录**(项目本身可能同时聚合主目录与若干工作树,见 §2·A「分组」,各目录可能在不同分支上)本文档尚未显式定义,是 UI 定稿时发现的真实歧义,不是本需求已拍板的定义,见 §8 开放问题 12。
- **代价(有意识的取舍,非被忽略的副作用)**:对话(根)行高度从现状单行(约 30px)变为两行(预计 46–48px 量级,精确值以 UI 定稿为准);项目行同样由单行变为两行,带来同类的纵向密度下降,具体数值待 UI 定稿。两类行同样意味着同样纵向空间内能看到的行数减少,这是有意拿「单行信息密度」换「状态 / 分支信息可以直接读出来、不必进一步交互查看」,必须明确记录、不得淡化;subagent 保持单行不受此影响,权衡只发生在对话(根)与项目层级。
- **已归档**的对话及其 subagent **不出现**在树中;**已关闭(closed)的对话保留显示**——对话(根)行的当前状态标签显示「空闲」,subagent 行的状态点归入空闲灰(不带光环 / 呼吸动效,同「静止两态」规则)——closed 只是进程结束、会话数据仍在且可经发消息复活,若从树上隐藏,用户将永久失去这些对话的唯一入口。**(发散验证修正 2026-07-12:原文「已关闭不出现」在 daemon 重启场景实测导致存活会话批量静默消失,QA③ 实证数据未丢仅不可见,故收窄为「只滤已归档」;待董事长晨间确认。)**

#### C. 树行交互(hover / 选中 / 工作区卡片 / 更多 / 双击重命名)

- **hover**:行底色变浅;项目行行尾浮出「新建对话」图标,对话 / subagent 行行尾浮出「更多」图标(点击打开与右键相同的菜单)。项目行**没有**行尾「更多」快捷入口——项目菜单只能经右键 / 长按打开。
- **hover 与选中为两档灰阶(相对现状单档的有意修正)**:hover = 较浅一档的淡灰,选中 = 较深一档的淡灰(两者均为柔和灰,不使用黑底白字 / 强调色做底),两档可清楚区分;选中态持续显示、不随指针移开而消失。现状 hover 与选中共用同一档灰,本轮改为两档,对齐 codePilot 设计系统下同类控件(目录树行、右栏页签)既有的两档灰阶惯例(见 §8 开放问题)。只有对话 / subagent 可被选中,项目节点不可选中(点击只切换展开 / 收起)。
- **hover 工作区卡片**(仅对话行,桌面网页端,短暂宽限延迟后出现、离开安全区后再延迟收起):浮出该对话的标题、当前分支、工作目录、最近变更时间;若该工作区存在改动统计 / 关联 PR / CI 检查结果,一并显示摘要;卡片内目录与分支可点击复制,检查摘要可点击跳转外部详情。
- **双击重命名**:对已绑定工作目录的对话行,300ms 内双击进入行内重命名(等价于右键菜单的「重命名」);行内变为输入框 + 聚焦态边框,回车提交、Esc 取消。

#### D. 右键菜单(两套,忠实还原现状)

- **项目行菜单(6 项)**:置顶 / 取消置顶 · 在 Finder 中显示(需 Electron 桌面壳 + 已知本地目录,否则不显示)· 创建工作树(需已知本地目录,否则不显示)· 重命名项目 · 归档(禁用占位,语义待定,不在本需求内裁定)· 移除(破坏性,二次确认,确认后仅从侧栏移除该项目分组,不触碰磁盘文件)。
- **对话 / subagent 共用菜单(11 项 + 分隔)**:置顶 / 取消置顶(需已绑定工作目录)· 重命名(需已绑定工作目录)· 归档(禁用占位)· 标记为未读(禁用占位)· —— · 在 Finder 中显示(条件同项目行)· 复制工作目录(禁用占位)· 复制会话 ID(始终可用,与是否绑定工作目录无关)· 复制深度链接(禁用占位)· —— · 派生到本地(禁用占位)· 派生到新工作树(禁用占位)· —— · 在新窗口打开(需 Electron 桌面壳 + 已知本地目录,否则不显示)。
- 对话与 subagent **共用同一套菜单**(不拆分出按「对话根 / subagent」区分的第三套)——这是现状,本需求原样保留;上述「禁用占位」项的具体语义**不在本需求裁定**,维持现状占位。

#### E. 新对话入口(两处)

- **全局入口**:树区域顶部固定一行「新对话」,点击导航进入既有新建对话流程,**不预选**项目。
- **项目内入口**:项目行 hover 时行尾浮出「新建对话」图标,点击进入同一流程,但**预选**该项目工作目录。
- 两处都只负责**导航到已有的新建流程**;该流程内部如何选工作目录 / provider / 模式等属另一能力,本需求不重画(深度切后续)。

#### F. 会话搜索入口

- 树区域顶部固定一行「搜索」(含快捷键提示),点击或按快捷键唤出既有的搜索呈现。
- 本需求要求的最低能力:**至少支持按对话名称过滤并定位候选**。既有搜索呈现若已覆盖更深范围(消息内容、目录等跨维度匹配),那是其自身既有能力,不在本需求重新设计或重新验收的范围内(深度切后续)。

#### G. 点击联动(中区 / 右侧)

- 点击一个**根对话**节点 → 中区切换焦点到该对话(发起侧;承接侧 = `2026-06-28-conversation-page`,目前仍是占位——本需求只保证「点击后中区确实切到了该对话的位置」,中区能否渲染出真实消息流由该需求独立验收)。
- 点击一个 **subagent** 节点 → **发起侧**:树内点击、触发跳转到右侧的动作在现状代码里真实存在、真实可用,不改变中区当前显示的根对话。**承接侧**:右侧最终呈现的具体页签形态,不超出 `2026-07-07-right-sidebar` 既有声明的范围——该需求把「对话」列为其统一页签框架下的一种 **deferred** 类型,尚未按新框架设计实现;本需求只保证「点击后确实在右侧发生了跳转」这一发起侧行为,**不承诺**右侧呈现已经是该需求定义的正式「对话」页签类型。端到端体验待该需求把「对话」页签类型落地后才算完整(见 §5)。
- 树的选中态准确反映「中区当前呈现的是哪个根对话」;选中某 subagent 不会清除其所属根对话行原有的选中标识(两者同时可见选中态)。

#### H. 主机切换器(顶部 · 完整闭环)

- 位于**左栏内容最顶部**(壳的窗口控制 / 统一顶栏归壳所有,这是「内容」自己的顶部,不是壳顶栏的一部分)——从当前(临时落在左栏底部、紧邻设置)的位置**挪到此处**,能力本身**原样复用**,不重新设计(见 §5、§8)。
- 显示当前主机名 + 三态连接点(在线 / 连接中 / 离线);点击展开下拉,列出全部已配置主机(各自连接点 + 名称),当前项有明确标识;末尾「添加主机…」入口(复用既有添加主机流程,不重画其内部)。
- 选中一台**在线**主机 → 整个工作区(树 + 中区 + 右侧面板)进入切换中过场,随后重载为该主机数据。
- 点击一台**离线**主机行 → **不直接切换**,原地对其发起重连尝试(退避重试);成功后该行状态转为在线,此时用户可再次点击真正切换过去(相对现状的一处行为修正,见 §8)。
- 仅有一台主机时,下拉只列出该主机 + 「添加主机…」。

#### I. 全态(空 / 加载 / 错误 / 离线 / 浅深主题)

- **空**:无任何项目和对话时,「项目」分组只显示新增入口,「对话」分组显示「暂无对话」提示;新对话 / 搜索 / 主机切换器三个入口仍正常可用,不是死胡同。
- **加载**:首次进入或切主机期间,树位置显示骨架占位行,完成后换入真实内容。
- **错误**:拉取项目或对话列表失败时,树区域出现错误提示 + 「重试」;点重试重新拉取。
- **离线**:当前主机断连 → 主机切换器状态点转离线、树区域出现「离线 · 重新连接」提示条;树内容冻结为断连前最后已知状态、操作项置灰(只读);点「重新连接」发起退避重试,恢复后按最新数据刷新。
- **浅 / 深主题**:读壳提供的 codePilot 主题来源;选中态与 hover 态均为柔和灰阶(两档,见 §2·C),不使用黑底白字 / 强调色做底。

### 深度切分 · 显式论证

> 为什么「树主体 + 主机切换器 + 两套右键菜单(现状还原)」全做,而「专属 subagent 菜单 / 拖拽排序 / 新建流程与搜索内部」只接入口或 deferred?

**① 树是唯一导航起点**——没有可用的树,用户无法进入任何对话,app 不可用;这是必须做全、不能碎片化交付的部分,没有「分批」的空间。

**② 现状即真实、成本最低、风险最小**——树的展示 / 行交互 / 两套右键菜单 / 主机切换器,在当前左栏里都是**真实上线、跑过用户反馈打磨**的行为。本需求要做的是「把这套已验证的真实行为原样接进新壳的左栏容器」,这是最低风险、立即可验收的路径——不是从零设计一遍,也因此不会返工。**例外**:状态点视觉与 hover / 选中的两档灰阶是本轮**新设计 / 有意修正**的两处,不在「现状照搬」之列,理由与范围见 §2·B、§2·C、§8。

**③ 深链接的内部流程不重复设计**——新建对话流程、搜索呈现,都是本身完整独立、已有明确产品形态的能力;本需求作为「消费方」只需接好入口,重新设计等于重复劳动,还可能与既有形态打架。

**④ 专属 subagent 右键与拖拽排序是锦上添花、非阻塞项**——「剥离为独立对话」这个能力已经在对话页的 subagent 相关区域有出口,不会因为树内暂缺专属菜单而让用户被卡住;拖拽排序从未实现过,也从未被证明是当前的刚需。两者都不影响「看树、选对话、做基本管理」这个核心闭环,归后续、不为它们拖慢本轮交付。

### 不做

- **本次(2026-07-23,含视觉修正与信息架构修正两轮)追加修正不无限扩大改动范围**:这两轮修正合计影响的是图标、状态点是否存在及渲染方式、标签行、对话(根)行与项目行是否为两行及两行内容(见 §2·A、§2·B);展开箭头、缩进逻辑、两套右键菜单、hover 尾部动作、内联重命名、closed 语义、拖拽等均不受影响,继续维持 2026-07-11 闸 1 通过时的定义。**项目行结构本身属信息架构修正这一轮的变动范围**(升级为两行),不再沿用视觉修正那一轮「项目行不受影响」的表述——这处衔接特此说明,避免两轮修正的边界描述互相矛盾。
- **不重定义壳行为**:左栏开关、默认 240px、拖 180–300px、按工作区记忆宽度、设置入口固定在左栏最下方槽位——**全部沿用**壳子 `2026-06-28-shell` 与 `2026-06-27-codepilot-layout`,本需求一概不改、不重写,只在文档里**引用**设置入口的存在。
- **不做树节点拖拽排序**——沿用 shell §7.b 第 16 条已定的 deferred,现状也未实现,本轮不新增。
- **不做按节点类型细分的第三套「subagent 专属」右键菜单**(跳到父对话 / 剥离为独立对话)——现状对话与 subagent 共用同一套菜单,本轮忠实还原现状,不新增专属菜单;home-shell 曾构想过这套专属菜单,本轮视为后续方向,不静默丢弃(见 §5)。
- **不裁定「归档 / 标记为未读 / 复制工作目录 / 复制深度链接 / 派生到本地 · 新工作树」等菜单项的语义**——现状即为禁用占位、语义待定,是与本需求无关的既有决策,本轮不重新打开这些讨论,维持现状占位。
- **不重画「新建对话」流程内部**(选目录 / 选 provider / 选模式等)——那是已有的独立流程,本需求只接入口。
- **不重画搜索 / 命令面板本身**——已有独立能力,本需求只接入口 + 保证「至少按名称可查到」的最低期望。
- **不做 provider 内部只读 subagent(如 Claude 原生 Task 子任务)的深层可见性**——那是 `2026-06-27-subagent-native-tree` 的范围(仍评审中);本需求的树结构对其保持开放(同一套嵌套 / 展开 / 状态点语义天然适配),但不在本需求内实现或验收其读取能力。
- **不做移动端 / 紧凑态**——桌面 only,紧凑态 deferred,沿用壳既定的「后续由桌面统一适配」安排。
- **不涉及协议 / RPC / 能力标志点名**——纯前端 WHAT 层,能力门只做「依赖某能力」的表述,不点名。

## 3. 流程(端到端)

- **落地默认态**:连上主机后,左栏内容自上而下依次呈现:主机切换器(顶部)→ 新对话 / 搜索(两行入口)→ 置顶组(若有)/ 项目组 / 对话组(按各自展开记忆呈现)。
- **浏览树**:点项目行(或其展开箭头)→ 展开 / 收起其下对话;点对话 / subagent 行的展开箭头 → 展开 / 收起其子代理(不触发联动);点对话 / subagent 行本身(非箭头区域)→ 选中并触发联动(见 §2·G)。
- **新建对话**:点顶部「新对话」→ 进入既有新建流程(不预选项目);或 hover 项目行 → 行尾「新建对话」→ 进入同一流程且预选该项目工作目录。流程完成后,新对话出现在树中对应位置并自动选中。
- **搜索**:点顶部「搜索」或按快捷键 → 唤出既有搜索呈现 → 输入内容按名称(至少)过滤定位候选 → 选中一项 → 关闭搜索、树滚动到该对话并选中、按 §2·G 联动规则呈现内容。
- **管理操作**:右键(或对话 / subagent 行的行尾「更多」)一行 → 按行类型弹出对应菜单 → 选项按现状可用性显隐 / 禁用 → 点击执行(置顶 / 重命名 / 在 Finder 中显示 / 创建工作树 / 移除项目 / 复制会话 ID / 在新窗口打开等);重命名走行内输入(回车提交 / Esc 取消),双击对话行是重命名的快捷方式。
- **切主机**:点主机切换器 → 下拉列出所有主机 → 点一台在线主机 → 整个工作区(树 + 中区 + 右侧面板)进入切换中过场 → 重载为该主机数据;点一台离线主机 → 不切换、原地触发重连(退避重试)→ 成功后该行转在线、可再次点击真正切过去;点「添加主机…」→ 进入既有添加主机流程,完成后新主机出现在列表且可选中。
- **边界分叉**(每态有入口有出口,无死胡同):
  - **空态**(无项目也无对话):树区域只显「项目」分组的新增入口与「对话」分组的「暂无对话」提示,顶部三个入口均可用。
  - **加载**:首次拉取 / 切主机期间,树位置显示骨架占位行,完成后换入真实内容。
  - **错误**:拉取项目或对话列表失败 → 树顶部出现错误提示 + 重试;点重试 → 重新拉取,成功则恢复正常态。
  - **离线**:当前主机断连 → 主机切换器状态点转离线红 + 树区域出现「离线 · 重新连接」提示条;树内容冻结为断连前最后已知状态(只读,操作项置灰);点「重新连接」→ 退避重试;恢复后树内容按最新数据刷新。
  - **管理操作进行中**:对某行发起归档 / 移除等操作后,该行进入处理中态,完成后从树消失(或按操作语义更新);失败则保留原状 + 反馈。

## 4. 屏与全状态

> 逐屏对应 [ui.html](./ui.html) 锚点 `ui.html#sCTx`,与 UI 设计师对齐锚点(见 §7)。共 **7 屏**(`sCT1`–`sCT7`,按 UI 定稿屏结构对齐:原 sCT4「右键菜单」并入 sCT3;原「点击联动」与「主机切换器」两屏互换顺序,主机切换器提前)。每屏完整交互态以 ui.html 逐屏「交互注解块」(布局 / 状态 / 右键菜单 / 点击效果)为准。布局维度统一:**桌面 only;移动 / 紧凑态沿用壳既定的 deferred 安排**。

- **sCT1 左栏整体默认态(结构与分组总览)** `ui.html#sCT1`:已连接主机、有项目和对话时的整体落地态——自上而下:主机切换器 → 新对话 / 搜索两行入口 → 置顶组(若有)→ 项目组 → 对话组;设置入口在更下方,由壳提供,本需求只引用其存在、不重画。状态:默认(有内容)/ 整体加载(骨架)/ 错误(顶部提示 + 重试)/ 离线(提示条 + 树冻结只读)/ 浅深主题。右键菜单:空白区域(树内容之外)无右键菜单。点击效果:见 sCT2–sCT7 各自细分,本屏建立整体骨架与分组顺序。
- **sCT2 对话树结构拆解(项目 → 对话 → subagent 无限嵌套)** `ui.html#sCT2`:树的核心嵌套形态——项目展开出对话(根),对话展开出 subagent,subagent 可再展开出更深一层,递归、不设人为层数上限。状态:每层默认 / hover / 选中;可展开(前置箭头,收起 / 展开图形不同)与不可展开(等宽占位、无箭头);对话 / subagent 节点**不再显示机器人图标**,项目节点文件夹图标不受影响;**对话(根)行不再有状态点**,前置图形只剩展开箭头,行内容为两行——第一行标题 + 最近说话相对时间(行尾,替代原后代计数角标)、第二行标签行(至少 CLI 类型标签 + 当前状态标签,文字呈现五态语义);**subagent 保持单行**,前置为小号纯色实心状态点(不带外圈光环,运行中 / 需关注 / 初始化中三态自身透明度呼吸、空闲 / 出错两态静止)+ 标题 + 后代总数角标(全部后代数,不止直接子节点);**项目行升级为两行**——第一行文件夹图标 + 标题不变,第二行当前分支 + diff 统计(无分支显示「暂无分支」)。项目展开但无对话时的「暂无对话」缩进提示;深层缩进达到上限后停止增长、仅靠展开箭头与父子关系判断更深层级(不再依赖图标)。**本屏三类行的信息架构 2026-07-23 经两轮修正(视觉方案 D → 信息架构修正),见 §2·A / §2·B / §8 开放问题 7–11**。右键菜单:见 sCT3。点击效果:点项目行(含箭头)= 展开 / 收起;点对话 / subagent 的箭头 = 仅展开 / 收起;点对话 / subagent 行本身 = 见 sCT6(联动)。
- **sCT3 树行交互全态(hover / 选中 / 工作区卡片 / 更多 / 双击重命名 / 右键菜单)** `ui.html#sCT3`:树行(subagent 单行,对话(根)与项目行均为两行——见 §2·B 2026-07-23 两轮追加修正)在各类交互下的呈现,hover / 选中 / 双击重命名等均覆盖整行、不因两行而改变判定方式,**本屏含右键菜单维度**(原 sCT4 并入此屏,项数与项名原样保留)。状态:默认 / hover(行底色变浅,项目行行尾浮出「新建对话」、对话与 subagent 行行尾浮出「更多」)/ 选中(**两档灰阶**——hover 较浅一档、选中较深一档,持续显示,相对现状单档的有意修正,见 §2·C / §8)/ 重命名中(行内输入框 + 聚焦态边框)。工作区卡片(仅对话行,桌面网页 hover 触发,带出现 / 消失的宽限延迟):标题 / 当前分支 / 工作目录 / 最近变更时间,附带时的改动统计 / PR / 检查状态摘要;目录与分支行可点复制。**右键菜单(两套,项数与条件见 §2·D)**:项目行菜单(6 项)——置顶 / 取消置顶 · 在 Finder 中显示(条件显隐)· 创建工作树(条件显隐)· 重命名项目 · 归档(禁用占位)· 移除(破坏性,二次确认);对话 / subagent 共用菜单(11 项 + 分隔)——置顶 / 取消置顶(需已绑定工作目录)· 重命名(需已绑定工作目录)· 归档(禁用占位)· 标记为未读(禁用占位)· —— · 在 Finder 中显示(条件显隐)· 复制工作目录(禁用占位)· 复制会话 ID(始终可用)· 复制深度链接(禁用占位)· —— · 派生到本地(禁用占位)· 派生到新工作树(禁用占位)· —— · 在新窗口打开(条件显隐,需 Electron 桌面壳);项目行**仅能经右键 / 长按**打开菜单(无行尾「更多」快捷入口)。点击效果:双击(300ms 内,仅已绑定工作目录的对话行)= 进入行内重命名;工作区卡片内目录 / 分支行点击 = 复制到剪贴板 + 短暂反馈;检查摘要点击 = 跳转外部详情;右键菜单逐项点击效果见 §2·D,禁用项点击无响应,分隔线无交互。
- **sCT4 新对话 + 搜索入口** `ui.html#sCT4`:左栏内容顶部固定的两行入口(全局「新对话」「搜索」,均带快捷键提示),以及项目行 hover 时行尾浮出的作用域内「新建对话」。状态:默认 / hover / 按下 / 快捷键提示常显。右键菜单:无。点击效果:「新对话」= 进入既有新建流程(不预选项目);项目行「新建对话」= 进入同一流程且预选该项目;「搜索」= 唤出既有搜索呈现,选中候选后的效果见 sCT6。
- **sCT5 主机切换器(顶部 · 下拉 · 切换全流程 · 离线重连)** `ui.html#sCT5`:左栏内容最顶部的主机切换胶囊及其下拉。状态:胶囊默认 / hover / 展开(下拉打开,边框高亮);下拉每行——在线(绿点)/ 连接中(橙点 + 态文案)/ 离线(红点 + 态文案,可点、语义是「重连」而非禁用)/ 当前项(明确标识,不显态文案);仅一台主机时下拉只列该主机 + 添加主机;切换中(过场指示)/ 整工作区重载(树 + 中区 + 右侧面板先出现页级 / 骨架级加载指示,完成后换入新内容)。右键菜单:无(下拉本身即菜单形态)。点击效果:点胶囊 = 展开 / 收起下拉;点下拉内在线主机行 = 关下拉 → 切换中过场 → 整工作区重载;点下拉内离线主机行 = **不关闭下拉**、原地对该主机发起重连尝试、行状态从离线过渡为连接中、成功后转在线(相对现状的行为修正,见 §8);点「添加主机…」= 进入既有添加主机流程,完成后新主机出现在列表且可选中。
- **sCT6 点击联动(根对话 → 中区;subagent → 右侧;选中态)** `ui.html#sCT6`:点击树上一行(或从搜索选中一项)后,内容在何处呈现、树自身如何反映「当前选中」。状态:未选中(树内无高亮)/ 已选中根对话(该行选中态灰底,持续到用户切换或该对话被关闭 / 归档)/ 已选中 subagent(该行选中态灰底,其所属根对话行**保持原有选中标识不丢失**)。点击效果:点根对话行 = 中区切换加载该对话(发起侧;承接侧见 §5);点 subagent 行 = **发起侧**在右侧触发跳转、不改变中区当前显示的根对话,**承接侧**(右侧最终呈现的具体页签形态)不超出 `2026-07-07-right-sidebar` 既有 deferred 声明的范围,端到端体验待该需求「对话」页签类型落地(ui.html 以醒目标记标注此「待接通」状态,沿用该需求断链表述范式,见 §2·G / §5);从搜索选中一项 = 等价于直接点击该行。
- **sCT7 全态合屏(空 / 加载 / 错误 / 离线 / 浅深主题)** `ui.html#sCT7`:一屏汇总树区域的边界状态,便于横向核对。状态:空(「项目」分组只有新增入口、「对话」分组显「暂无对话」;三个顶部入口仍可用)/ 加载(骨架占位行)/ 错误(顶部提示框 + 重试)/ 离线(提示条「主机离线 · 重新连接」+ 树冻结只读)/ 浅色与深色两套并列(均读壳的 codePilot 主题来源,选中 / hover 均为两档灰阶——见 sCT3)。右键菜单:空 / 加载 / 错误 / 离线态下树行本身要么不存在要么只读,右键不可用或不产生动作。点击效果:「重试」= 重新拉取;「重新连接」= 退避重试连接。

## 5. 边界与依赖

- **与 shell(`2026-06-28-shell`)的关系**:shell 定义左侧边栏这个空容器的**外壳行为**(展开 / 收起 toggle、默认 240px、拖 180–300px、按工作区记忆宽度、设置入口固定在左栏最下方槽位)——本需求**完全不改**这些,只负责往这个已经会开关、会拖宽的容器里填入内容。本需求正是该需求 §7.b 第 7–18 条点名的「后续内容里程碑」的兑现者之一。
- **与 codepilot-layout(`2026-06-27-codepilot-layout`)§5 的关系**:该需求明令「左栏外壳(开关 / 位置 / 240px / 拖 180-300 / 主题)归 CodePilot,内部内容 + 布局归 Helm 现有那套对话树」,并逐项点名了本需求要交付的清单(主机切换器 / 新对话 / 搜索 / 对话树细节 / 设置引用)。本需求就是把该判决落成可验收的 requirement;不采用 CodePilot 左栏的插件 / 素材库内容(该判决已排除)。
- **与 home-shell(`2026-06-25-home-shell`)的关系**:home-shell 是**尚未落地的概念稿**(gate-1 未过、未回写 canonical)。本需求**不是**把 home-shell 搬过来,而是以**当前真实实现**为准,只在一处采纳 home-shell 已验证过的设计意图作为改进(离线主机点击语义,见 §8);home-shell 里尚未实现的部分(拖拽排序、subagent 专属右键的「跳到父对话 / 剥离为独立对话」、目录节点的「移动到目录 / 复制对话」等)本需求视为后续方向,不在本轮实现,不代表被放弃。
- **与 subagent-native-tree(`2026-06-27-subagent-native-tree`,评审中)的关系**:该需求负责让 provider 内部只读 subagent(如 Claude 原生 Task 子任务)在树中任意层可见并可点开只读对话,目前仍在评审、未实现。本需求的树结构(无限嵌套 / 展开收起 / 状态呈现 / 角标)设计上对其保持开放——同一套语义天然可承载只读节点,不需要本需求预先改动;但本需求的验收范围**不包含**provider 内部子的深层可见性本身,那是该需求自己的验收范围。
- **与 conversation-page(`2026-06-28-conversation-page`)的关系**:本需求点击根对话后「发起」中区切换,但中区**承接**这条对话、渲染出消息流,是 conversation-page 的范围;该需求目前仍是占位(尚未实现消息流内容)。本需求的验收只要求「点击后中区的焦点确实切到了该对话」,不要求中区已经能渲染出真实消息流——那由 conversation-page 自己独立验收。
- **与右侧边栏(`2026-07-07-right-sidebar`)的关系**:subagent 点击 → 右侧打开对话,是**发起侧真实存在、承接侧 deferred** 的一条断链(表述范式同该需求 §2·D 的三条断链)——**发起侧**(树内点击、触发跳转到右侧)在现状代码里真实可用;**承接侧**(右侧以该需求定义的「对话」页签类型正式承接)仍是 deferred,该需求明确「对话(右栏侧边会话 / 子对话,deferred)」尚未按其统一页签框架设计实现。本需求的验收只覆盖发起侧,不对承接侧的最终呈现形态做超出该需求既有声明的承诺;两者何时端到端接通,取决于该需求「对话」页签类型的落地时间,不在本需求内处理。
- **能力门(不点名协议 / RPC)**:依赖「能拿到项目分组」「能拿到对话 / subagent 的父子关系与状态」「能对某主机发起连接 / 重连」「能创建 / 重命名 / 置顶 / 移除项目或对话」「能拿到工作区的分支 / 目录 / 改动 / PR / 检查信息,用于 hover 卡片、以及本轮项目行第二行的常显分支 / diff」这类既有能力;本轮新增两类能力依赖——「能拿到对话所属的 agent provider 归属」「能拿到对话最近一次消息往来的时间」——两者是否已可用、能否本轮接入均待架构确认(见 §8 开放问题 8、7),不预先假定已具备。本需求只读取、只展示、只发起既有动作,不新增采集或协议机制。旧主机若缺少某项能力,按现有「升级主机」提示处理(不点名具体能力名)。
- **平台**:桌面 only。移动 / 紧凑态不在本轮,沿用壳既定的「deferred、后续由桌面统一适配」安排(左栏在紧凑态本就是不同呈现,不受本需求约束)。
- **术语**(对齐 [glossary.md](../../../glossary.md)):「对话」= 一个根 agent 的会话(Agent session);「项目」= 按代码仓库聚合的分组;「subagent」= 有父对话的会话,可再嵌套自己的 subagent;「主机」= 客户端连接的一个 daemon profile。
- **无老用户 / 迁移**(fork 未上线)。**不改协议 / RPC / `server_info.features.*`**:本需求是纯前端的内容填充。

## 6. 验收标准(逐条可判定 · 能 / 否)

**树结构与嵌套**

1. 左栏内容自上而下依次呈现:主机切换器 → 新对话 / 搜索两行入口 → 置顶组(仅当存在置顶项时出现)→ 项目组 → 对话组(游离对话)。
2. 树呈现项目 → 对话(根)→ subagent → … 的嵌套关系,层数不设人为上限;有可展开内容的节点前置展开箭头,收起态与展开态图形不同。
3. 项目节点展开但其下没有任何对话时,显示「暂无对话」的缩进提示,而不是空白或消失。
4. 对话 / subagent 节点只有真正存在子节点时才显示展开箭头;无子节点的行以等宽占位对齐、不显示箭头。
5. subagent 节点在拥有后代时显示后代总数角标(不止直接子节点数);对话(根)节点**不再**显示这一角标,其第一行行尾改为「最近说话相对时间」(2026-07-23 信息架构修正,见 §2·A、§2·B、§8 开放问题 9)。
6. 深层嵌套超过一定层数时,视觉缩进停止继续增长,但展开 / 收起与父子关系不受影响、仍可继续操作更深层级。

**状态点、标签行与对话 / 项目行结构**

7. 每个对话 / subagent 节点的运行状态,至少能区分运行中 / 待输入或需关注 / 空闲 / 出错 / 初始化中五种,且与应用内其它活动指示使用同一套语义,而非本树自造的新状态词汇;五态语义本身不因视觉表达方式的差异(状态点 vs. 文字标签)而改变。
8. 对话 / subagent 节点均不再显示机器人图标;项目节点的文件夹图标不受影响。
9. 对话(根)节点不再显示状态点——前置图形位置只剩展开箭头(如有子代);运行状态改由该行标签行中的「当前状态标签」以文字呈现(2026-07-23 信息架构修正)。
10. subagent 节点保留状态点(小号纯色实心圆,不带外圈光环 / 阴影光晕);运行中 / 待输入或需关注 / 初始化中三态以状态点自身透明度呼吸表达动效,空闲 / 出错两态静止,不做动画。
11. 对话(根)节点的行由单行改为两行:第一行 = 标题(单行、超长省略)+ 最近一次消息往来的相对时间(行尾);第二行 = 标签行,由若干独立标签组成,不是一整句状态描述文字。两行同属一个交互单元,hover / 选中 / 双击重命名等既有行为覆盖整行、不因两行而拆分。
12. 标签行至少包含两个标签:CLI 类型标签(显示驱动该对话的 agent provider)与当前状态标签(以文字呈现五态语义);「至少」意味着不排斥后续增加更多标签,本需求不因此认为标签行已经封顶。
13. 当前状态标签的文案至少满足:运行中 → 「运行中」;需关注态由待处理的权限确认触发时 → 「等待权限确认」,由其它触发原因(回合结束等待用户、完成后需要人复核)触发时 → 「等待你的回复」;空闲 → 「空闲」;出错 → 「出错」;初始化中 → 「初始化中…」;这些文案所需数据复用既有字段,非新增数据缺口。
14. CLI 类型标签需显示正确的 provider 归属;该数据当前能否从对话树自身数据模型取得尚待架构确认(见 §8 开放问题 8),确认前不假定数据已就绪、本条暂不作为强制验收项。
15. 对话(根)行第一行行尾的「最近说话相对时间」需反映真实的最近一次消息往来时间,且不分状态、每一行都要展示;该数据当前对话树自身数据模型是否已具备尚待架构确认、且为阻塞性依赖(见 §8 开放问题 7),结论落地前本条暂不作为强制验收项。
16. 对话(根)行不再显示子对话(subagent)总数角标——按字面直接去除,不移到别处;此信息去留的可用性顾虑已独立列为开放问题(见 §8 开放问题 9),不在本条验收范围内擅自恢复或裁定。
17. subagent 节点本轮维持不变:继续单行呈现,不采用两行结构;不显示机器人图标;后代总数角标逻辑不变(拥有后代时才显示)。
18. 项目节点的行由单行改为两行:第一行(不变)= 展开箭头(如有子代)+ 文件夹图标 + 标题(单行、超长省略);第二行(新增)= 当前 git 分支,若该工作区存在未提交改动则在分支名后附加增删行数统计,无改动时只显示分支名,无分支时显示「暂无分支」且不叠加 diff 统计。
19. 文档明确记录对话(根)行与项目行两行化带来的纵向密度下降(相对现状单行,同样纵向空间内可见对话 / 项目行数减少),将其作为有意识的取舍披露,而非未提及的副作用。
20. 已归档的对话及其 subagent 不出现在树中;已关闭(closed)的对话保留显示——对话(根)行的当前状态标签显示「空闲」,subagent 行的状态点归入空闲灰(见 §2·B 发散验证修正注)。

**行交互**

21. 行默认态、hover 态(行底色变浅 + 行尾浮出操作)、选中态(与 hover 呈**两档灰阶**——hover 较浅、选中较深一档,均不使用黑底白字 / 强调色做底)均可判定区分,且 hover 与选中两档可清楚分辨(相对现状单档灰的有意修正)。
22. hover 对话行(桌面网页端)时,经短暂宽限延迟浮出工作区卡片,显示标题 / 当前分支 / 工作目录 / 最近变更时间;若该工作区存在改动统计 / 关联 PR / CI 检查结果,一并显示;卡片内目录与分支可点复制。
23. 项目行 hover 时行尾浮出「新建对话」图标;对话 / subagent 行 hover 时行尾浮出「更多」图标,点击打开与右键相同的菜单。
24. 双击已绑定工作目录的对话行,300ms 内触发行内重命名(等价于菜单里的「重命名」);行内重命名态为输入框 + 聚焦态边框,回车提交、Esc 取消。
25. 树节点拖拽排序**不在本轮范围**,文档明确标注为 deferred(不静默丢弃)。

**右键菜单**

26. 项目行右键菜单固定 6 项:置顶 / 取消置顶、在 Finder 中显示(条件显隐)、创建工作树(条件显隐)、重命名项目、归档(禁用占位)、移除(破坏性、二次确认、确认后仅从侧栏移除、不动磁盘文件)。
27. 对话与 subagent 行共用同一套右键菜单(不拆分出第三套专属 subagent 菜单),含:置顶 / 取消置顶、重命名(以上两项需已绑定工作目录)、归档(禁用占位)、标记为未读(禁用占位)、在 Finder 中显示(条件显隐)、复制工作目录(禁用占位)、复制会话 ID(始终可用)、复制深度链接(禁用占位)、派生到本地(禁用占位)、派生到新工作树(禁用占位)、在新窗口打开(条件显隐,需 Electron 桌面壳)。
28. 上述禁用占位项的具体语义**不在本需求裁定**,文档明确其为既有的、独立于本需求的待定项,维持现状占位、不新增也不删除。
29. 「跳到父对话」「剥离为独立对话」等 home-shell 构想过的 subagent 专属菜单项,本轮**不实现**,文档明确标注为后续方向、给出指针,不静默丢弃。

**新对话与搜索**

30. 左栏内容顶部存在一个不预选项目的全局「新对话」入口,点击后导航进入既有新建对话流程。
31. 每个项目行在 hover 时可发起「该项目下新建对话」,效果等同全局入口但预选该项目工作目录。
32. 左栏内容顶部存在一个「搜索」入口(含快捷键提示),点击或按快捷键唤出既有搜索呈现;搜索至少支持按对话名称过滤并定位候选。
33. 从搜索选中一个候选对话后,搜索关闭、树滚动到该对话并选中、内容按 §2·G 联动规则呈现。

**点击联动**

34. 点击一个根对话节点后,中区切换焦点到该对话(发起侧完整;中区自身能否渲染出真实消息流由 conversation-page 独立验收,不在本需求验收范围)。
35. 点击一个 subagent 节点后,**发起侧**——树内触发跳转到右侧、且中区当前显示的根对话不受影响、不切换——完整可验;**承接侧**(右侧最终呈现的具体页签形态)不超出 `2026-07-07-right-sidebar` 既有 deferred 声明范围,不在本需求验收范围内。
36. 树的选中态准确反映「中区当前呈现的是哪个根对话」;选中 subagent 不清除其所属根对话行的选中标识。

**主机切换器**

37. 左栏内容最顶部存在主机切换胶囊,显示当前主机名与三态连接点(在线 / 连接中 / 离线)。
38. 点击胶囊展开下拉,列出全部已配置主机(各自连接点 + 名称),当前项有明确标识;末尾有「添加主机…」入口。
39. 点击下拉内一台在线主机后,整个工作区(树 + 中区 + 右侧面板)经过切换中过场后重载为该主机数据。
40. 点击下拉内一台离线主机,**不直接切换**,而是原地对该主机发起重连尝试;重连成功后该行状态转为在线,此时可再次点击真正切换过去。
41. 仅有一台主机时,下拉只列出该主机与「添加主机…」。
42. 点击「添加主机…」进入既有添加主机流程,完成后新主机出现在列表中并可选中。

**全态**

43. 无任何项目和对话时,「项目」分组只显示新增入口、「对话」分组显示「暂无对话」提示;新对话 / 搜索 / 主机切换器三个入口在此空态下仍可正常操作,不是死胡同。
44. 首次进入或切主机期间,树位置显示骨架占位行,完成后换入真实内容。
45. 拉取项目或对话列表失败时,树区域出现错误提示与「重试」操作;点重试重新拉取。
46. 当前主机断连时,主机切换器状态点转为离线、树区域出现离线提示条;树内容冻结为断连前最后已知状态且操作项置灰(只读);点「重新连接」发起退避重试,恢复后树按最新数据刷新。
47. 浅色与深色两套主题均正确呈现,选中态与 hover 态均为柔和灰阶(两档,读壳的 codePilot 主题来源),不使用黑底白字 / 强调色做底。

**边界与范围声明**

48. 文档明确本需求**桌面 only**,移动 / 紧凑态标注 deferred、沿用壳既定安排。
49. 文档明确不重定义壳的展开 / 收起 / 宽度 / 拖拽 / 记忆 / 设置入口位置——这些行为完全沿用 `2026-06-28-shell` 与 `2026-06-27-codepilot-layout`。
50. 文档明确不重画「新建对话」流程内部与「搜索 / 命令面板」本身——两者均为既有独立能力,本需求只接入口。
51. 文档明确 provider 内部 subagent 的深层可见性属 `2026-06-27-subagent-native-tree` 独立范围,不在本需求验收内;本需求的树结构对其保持开放、不需预先改动。
52. 文档明确本需求不改协议 / RPC / `server_info.features.*`,能力门只做「依赖某能力」的 WHAT 级表述、不点名。
53. 文档明确本次追加修正(2026-07-23,含视觉修正与信息架构修正两轮)只影响图标、状态点是否存在及渲染方式、标签行、对话(根)行与项目行是否为两行及两行内容,不改变展开箭头、缩进逻辑、两套右键菜单、hover 尾部动作、内联重命名、closed 语义与拖拽等既有定义——这些内容延续 2026-07-11 闸 1 通过时的范围;项目行结构本身(是否两行)属信息架构修正这一轮的变动范围,不再维持「不受影响」。

**屏对齐**

54. sCT1–sCT7 共 7 屏在 ui.html 均有对应锚点,requirement 每状态 ↔ 屏 1:1,注解块覆盖布局 / 状态 / 右键菜单 / 点击效果四维度。

## 7. 给 UI 设计师的屏对齐清单

> 本需求与 UI 设计师并行产出,本轮已按总监对齐意见收口为 **7 屏**(与 UI 定稿屏结构对齐),双方共享本节作为 `sCT1`–`sCT7` 的锚点约定(Conversation Tree 前缀)。

| 锚点 | 屏名                     | 关键态(至少覆盖)                                                                                                                                                                                                                                                                                                                                          |
| ---- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| sCT1 | 左栏整体默认态           | 默认结构顺序(主机切换器 → 新对话 / 搜索 → 置顶 / 项目 / 对话三组)、加载骨架、错误提示、离线提示条、浅 / 深主题                                                                                                                                                                                                                                            |
| sCT2 | 对话树结构拆解           | 展开 / 收起(前置箭头)、去机器人图标(仅项目留文件夹图标)、**对话(根)去状态点**(前置只剩箭头,2026-07-23 信息架构修正)、subagent 保留小号状态点(纯色无光环 · 三态透明度呼吸)、对话(根)两行(标题 + 最近说话时间 / 标签行:CLI 类型 + 当前状态)、项目行两行(文件夹图标 + 标题 / 分支 + diff)、深层缩进封顶、subagent 后代计数角标、项目展开无对话的「暂无对话」 |
| sCT3 | 树行交互全态(含右键菜单) | 默认 / hover / 选中(**两档灰阶**)、对话(根)与项目行两行整行为一个交互单元(subagent 单行)、工作区卡片、行尾「更多」、双击重命名、项目菜单 6 项、对话 · subagent 共用菜单 11 项 + 分隔                                                                                                                                                                      |
| sCT4 | 新对话 + 搜索入口        | 全局新对话 / 项目内新建对话(hover 触发)、搜索入口 + 快捷键提示                                                                                                                                                                                                                                                                                            |
| sCT5 | 主机切换器               | 胶囊默认 / hover / 展开、下拉在线 / 连接中 / 离线三态、切换中过场 + 整工作区重载、离线行点击 = 重连非直接切走、添加主机入口                                                                                                                                                                                                                               |
| sCT6 | 点击联动                 | 根对话 → 中区切换、subagent → 右侧发起跳转(**承接侧 deferred,醒目标记**)、选中态双态共存                                                                                                                                                                                                                                                                  |
| sCT7 | 全态合屏                 | 空(项目 / 对话双空)、加载骨架、错误 + 重试、离线 + 冻结只读、浅 / 深主题并列(两档灰阶)                                                                                                                                                                                                                                                                    |

## 8. 开放问题(闸 1 六项已决;第 9、10 项董事长已确认收口;第 7、8、11、12 项待架构 / 产品进一步确认)

> **【董事长闸 1 · 2026-07-11 认可】**:董事长审阅设计稿后确认「没问题」——六条开放问题**全部按推荐值采纳**:①离线主机行点击=原地重连 ✓;②subagent 专属右键本轮不拆 ✓;③主机切换器完整闭环 ✓;④搜索接既有 ⌘K 能力 ✓;⑤状态点重新设计(五态)获过目认可 ✓;⑥hover/选中两档灰阶 ✓。**P1 闭环,进入 P2 架构。**下方条目保留为决策记录。
>
> **【董事长追加确认 · 状态点 / 图标二轮修正 · 2026-07-23】**:闸 1 通过后,董事长在总监的直接对话里针对第⑤项「状态点重新设计」追加一轮修正意见——树行现状的机器人图标与状态点堆在一起显得潦草,状态点原本设想的光环效果也没有达到预期。与总监反复打磨可视化方向稿(A/B/C/D 四个方向的对比)后,明确拍板采用**方案 D**:去机器人图标 · 状态点改无光环纯色实心圆 · 运行中 / 需关注 / 初始化中三态透明度呼吸 · 对话(根)行两行 + 可读状态文案(按触发原因拆成「等待权限确认」/「等待你的回复」两种)· subagent 保持单行。**上述均已拍板,不是待议项**,已回写 §2·A / §2·B / §4 / §6 / §7。(注:本 callout 记录的「对话(根)行两行 + 可读状态文案」这一表述,在下方信息架构修正 callout 里被进一步细化——第二行不再是一整句状态文案,而是标签行;第 7 条随之从「可选后缀」升级为阻塞性依赖。)
>
> **【董事长追加确认 · 信息架构三轮修正 · 2026-07-23】**:董事长看过 UI 实际渲染页面后,认为两行结构方向对但内容需要再深一层——这轮改的是信息架构:对话(根)行去状态点(只剩展开箭头)、第二行改标签行(至少含 CLI 类型 + 当前状态两个标签)、第一行行尾从子对话计数角标改成最近说话相对时间;项目行升级两行,第二行显示当前分支 + diff 统计。**上述四点已拍板,不是待议项**,已回写 §2·A / §2·B。**仍待确认 / 待架构确认的是下方第 8–11 条**:CLI 类型标签的数据依赖(8)、子对话计数信息去留的可用性顾虑(9)、对话(根)与 subagent 状态视觉表达不统一的张力(10)、分支 / diff 行内常显的性能代价(11);第 7 条(最近说话时间的数据依赖)地位由上一轮的「可选后缀」升级为本轮的「阻塞性依赖」,措辞已同步更新。
>
> **【董事长确认 · 开放问题 9 / 10 收口 · 2026-07-23】**:剩余两条待拍板的开放问题,董事长已给出明确结论——⑨**子对话计数角标维持去掉,不找补到别处** ✓(理由:运行中的 subagent 本来就会把状态贡献给父对话的聚合状态,「有 subagent 需要关注」这个信号不会因为去掉计数而消失,只是丢了「具体几个」这个数字,该数字本身价值不大);⑩**对话行去点、subagent 行留点维持现状,不强行统一** ✓(定性为有意的信息密度梯度——项目行最重「两行 + 分支信息」、对话行次之「两行 + 标签」、subagent 行最轻「单行 + 一个点」,层级递减是合理的视觉逻辑,不是疏漏)。第 9、10 条正文保留为决策记录,§8 现只剩第 7 / 8 / 11 / 12 项待架构 / 产品进一步确认。

1. **离线主机行点击语义修正**:现状(当前左栏)离线主机行点击与在线行一样直接尝试切换;本需求采用 `2026-06-25-home-shell` 已验证过的设计——改为「原地触发重连、不直接切走」(见 sCT5)。这是相对现状的一处行为修正,请董事长确认是否认可。若不认可,退回现状「点击即尝试切换」亦可,不影响其余范围。
2. **subagent 专属右键菜单(跳到父对话 / 剥离为独立对话)是否本轮纳入**:现状对话与 subagent 行共用同一套右键菜单,没有专属项;「剥离」能力已在对话页的相关区域有出口,不会因树内暂缺而阻塞用户。推荐:**本轮不纳入**,归后续(见 §5)。请董事长确认,或要求提前纳入。
3. **主机切换器采用完整闭环 vs 占位**:本需求选择复用现状已真实工作的主机切换能力(胶囊 + 下拉 + 切换 + 添加主机),仅将其从当前的「左栏底部(临时位置)」挪到「左栏内容顶部」。推荐:**采用完整闭环**(成本低、无需重新设计,只是挪位置)。请董事长确认是否认可挪动位置,或希望暂缓挪动、维持现状位置直到统一顶栏未来长出专属槽位为止。
4. **搜索入口的落点**:搜索点击后唤出的是现有的、跨主机 / 跨内容(标题 + 消息内容 + 目录)的搜索呈现,并非本需求新造一个只按名称过滤的轻量搜索框。推荐:**维持现状搜索呈现,不新造**,本需求只保证「至少按名称能查到」这个最低期望被满足(现状已满足)。请董事长确认这个「接入口、不重造」的处理是否符合预期,或希望本轮就单独做一个更轻量的名称过滤框(与现状搜索呈现二选一并存)。
5. **状态点视觉为重新设计,非现状复用(请特别过目视觉效果)**:现有代码曾经尝试过树内状态图标,但因反馈「达不到想要的效果」被整体移除;节点的运行状态数据(五值状态枚举 + 「需关注」标志)本身真实存在,只是当前没有对应的树内视觉。本轮据此重新设计一套五态状态点(运行中绿脉冲 / 待输入或需关注醒目色 / 空闲灰 / 出错红 / 初始化中过渡色,见 sCT2 图例)。请董事长闸 1 **特别过目视觉效果**(而非仅审 WHAT 文字描述),确认这版重新设计是否解决了此前「达不到想要的效果」的问题;若仍不理想,请指出具体不满意点以便下一轮调整。
6. **hover 与选中改两档灰阶**:现状 hover 与选中共用同一档淡灰,本轮改为两档——hover 较浅、选中较深一档(见 §2·C / sCT3)。这是相对现状的**有意修正**,理由是对齐 codePilot 设计系统下同类行 / 页签控件(目录树行、右侧栏页签)的既有两档灰阶惯例,让全产品在「悬浮」与「选中」的视觉区分上保持一致。推荐:**采纳**。请董事长确认。
7. **「最近说话相对时间」的数据依赖(2026-07-23 二次修正,地位由「可选后缀」升级为「阻塞性」)**:上一轮(视觉修正)此项还只是空闲态的一个可选后缀,不阻塞验收;本轮(信息架构修正)把它挪到对话(根)行**第一行行尾、每一行都要展示**,取代原有的子对话计数角标——地位从「锦上添花」变成「核心信息」,不显示等于这一行的信息架构缺了一块。数据现状不变:对话树自己的节点数据模型目前只带「创建时间」,不带「最近一次活跃 / 更新」时间;应用内其它地方(如工作区详情)确实已经在用类似「最近变更时间」的概念,理论上可能有可复用的数据来源,但本对话树模块尚未接入,能否接入、接入成本多高,requirement 层无法断定。**与上一轮不同,本轮要求架构给出的结论必须在本轮就明确**(不能像上一轮那样延后为不确定的「后续待办」)——若架构确认这项数据本轮拿不到,意味着对话(根)行第一行行尾这个位置暂时留白或需要产品重新决定放什么,请架构给出明确结论以便决策,不要默认「反正上一轮已经这么写过」就沿用旧结论。
8. **CLI 类型标签(provider)的数据依赖(2026-07-23 新增,待架构确认)**:董事长初步判断对话树自己的节点数据模型目前没有 provider 字段,认为这跟「最近说话时间」是同一类缺口——协议层的原始数据大概率带着 provider 信息,只是没有被这一层模块接住,但这只是初步判断,不是确认过的结论。**我独立重新核实过一遍(读了该模块的数据模型与解码逻辑,不是照抄这一判断)**:确认属实——协议层的原始快照数据确实带 provider 信息,但对话树自己的节点数据模型目前没有解出这个字段,不能直接拿来用。能否把它接进来、接入成本多高,需架构阶段确认。**本需求的强制要求只是标签行本身必须存在、且至少含 CLI 类型标签的位置**(见 §6 验收标准第 12 条);若架构确认本轮无法接入 provider 数据,请给出明确结论——是先只显示当前状态标签、CLI 类型标签延后,还是有其它更低成本的替代方案,由总监 / 董事长依据架构结论再定,不在此处预先假设。
9. **子对话(subagent)计数信息去留的可用性顾虑(2026-07-23 新增 · 董事长已确认收口)**:董事长原话是「不显示子对话的个数」,没有提到挪到别处;本次修正按字面理解执行为**直接去掉**,不做「移到别的位置」处理,行内不再有任何位置显示这个数字(见 §2·A、§2·B、§6)。这条信息此前是用户判断「这个对话底下挂了几个 subagent」的**唯一**途径,完全去掉是否构成可用性倒退,曾如实标为顾虑、未替董事长做取舍。**【已收口】董事长确认:维持「彻底去掉,不做任何补偿」**——理由:运行中的 subagent 本来就会把状态贡献给父对话的聚合状态,「有 subagent 需要关注」这个信号不会因为去掉计数而消失,只是丢了「具体几个」这个数字,该数字本身价值不大。§2·A、§2·B、§6 当前的正式内容(彻底去掉、不挪位置)即为最终定义,不再是待验证的基线。
10. **对话(根)行去状态点、subagent 行留状态点,同一棵树内视觉表达不统一(2026-07-23 新增 · 董事长已确认收口)**:本轮反馈未提及 subagent 行,按指示维持上一轮定义(单行 · 小号状态点 · 无机器人图标,见 §2·A、§2·B)不变。改动后,同一棵树里,对话(根)行的运行状态用**文字标签**表达(标签行的一部分),subagent 行的运行状态仍用**彩色圆点**表达——两种节点类型对同一套五态语义采用两种不同的视觉手法,曾如实标为一处真实张力、未自行拍板。**【已收口】董事长确认:维持现状,不强行统一**——定性为**有意的信息密度梯度**:项目行最重(两行 + 分支信息)、对话行次之(两行 + 标签)、subagent 行最轻(单行 + 一个点),层级递减是合理的视觉逻辑,不是疏漏。此前列出的「维持现状分层」方向即为最终结论,「subagent 同步升级为文字标签」不采纳。
11. **项目行分支 / diff 常显于行内的渲染 / 滚动性能代价(2026-07-23 新增,待架构确认)**:项目行第二行(当前分支 + diff 统计)所需的数据本身不是新缺口——工作区详情模型已有对应字段(可空的分支、可空的增删统计),现有的 hover 工作区卡片已经在用这两个字段、也已经处理了「无分支」的情况;这轮的性质更像是「把已经存在、原本只在 hover 时才计算 / 展示的信息,改成每个项目行常驻显示」。但常驻显示是否会给长列表的渲染或滚动带来额外性能代价(尤其项目数量多、diff 统计需要实时或准实时更新时),不归产品判断,需要架构阶段评估并给出结论;若代价过高,可能需要退让(比如降低刷新频率、或只在可视区域内计算),具体如何取舍由架构提出方案,本需求只要求「有分支时显示分支 + diff 统计、无分支时显示"暂无分支"」这个用户可见效果,不预设实现路径。
12. **项目行第二行"当前分支"对应哪个目录(2026-07-23 UI 定稿阶段发现,待产品 / 架构确认)**:§2·A「分组」已经写明「同一项目下可能同时有主目录与若干工作树,其上的对话都归在同一项目节点下」——项目节点本身就是多个目录的聚合。但项目行第二行只有一处位置显示「当前分支」(见 §2·A、§2·B),主目录和各工作树完全可能处在不同分支上,本文档此前没有显式定义这一处「当前分支」具体取哪个目录的分支。UI 设计师画稿时按**项目主目录自身的分支**处理(各工作树自己的分支仍由其对应对话行的 hover 工作区卡片单独展示,见 §2·C),但这只是绘稿时为了能往下画而做的假设,不是本需求已经拍板的定义,**不应由 UI 自行悄悄定案**。**这不是我能替产品 / 架构做的取舍,如实标出**:是否就按「项目主目录分支」这个假设定稿?主目录本身不存在或未绑定本地目录时这一行怎么处理?多个工作树分支不一致时,是否需要在项目行给出「多分支」一类的提示而不是默默只显示主目录一个?请产品(可能是我下一轮据此定稿)与架构(数据是否已能区分「项目下具体是哪个目录」)共同确认口径,本条目前不裁定。
