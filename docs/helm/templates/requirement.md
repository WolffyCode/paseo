<!-- 模板：复制到 docs/helm/requirements/<YYYY-MM-DD>-<topic>/requirement.md -->

# 需求 · <topic>

> 日期：<YYYY-MM-DD> · 状态：<草拟/评审中/已批/开发中/测试中/已验收> · 配套 UI：[ui.html](./ui.html) · canonical：docs/helm/product.md
> **只写 WHAT**。禁止真实代码片段、函数名、文件改法、实现路子（见 [standards.md](../../standards.md) §7）——那是架构师与开发者的事。

## 1. 目标 & 用户价值

一句话目标 + 用户为什么需要它（场景）。

## 2. 范围

- **做**：本需求覆盖的功能点。
- **不做**：明确划出去的（防范围蔓延）。

## 3. 流程

端到端：用户操作 → 系统响应，逐步。覆盖正常路径 + 分叉（失败/取消/降级）。

## 4. 屏与全状态

逐屏（引用 ui.html 锚点 `ui.html#sX`）。每屏覆盖：默认 / hover / 活跃 / 运行 / 禁用 / 空 / 加载 / 错误 / 离线 / 右键菜单（几个按钮+点击效果） / 每个可点元素的点击效果。

## 5. 边界与依赖

依赖什么能力（能力门）；平台差异（桌面/Web/手机）；与现有功能的关系。

## 6. 验收标准

可测的成功条件清单（测试逐条核）。每条写成「能/否」可判定的句子。
